

export const selectSettings = (state) => state.settings;

export const selectShowTime = (state) => state.settings.showTime;

export const selectTimeshiftActive = (state) => state.settings.timeshiftActive;

export const selectAlarmsActive = (state) => state.settings.alarmsActive;