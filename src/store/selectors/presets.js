

export const selectPresetBlocks = state => state.presets.blocks;

export const selectPresetTimeshift = state => state.presets.timeshift;

export const selectPresetSchedules = state => state.presets.schedules;

export const selectPresetErrors = state => state.presets.errors;

export const selectPresetLoading = state => state.presets.loading;
