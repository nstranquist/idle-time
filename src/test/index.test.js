/*
  - "Describe" used to group tests that are related
  - "It" blocks make an assertion about a particular fact about the test subject
  - "Expect" makes a single assertion about a specific case
    - In expect, there is "compnent -> matcher -> target value"
    - i.e. "expect(component).to.contain.(value)"

  - 'beforeEach' is a function that runs before every function that runs
    - can provide dummy props in beforeEach function

*/
